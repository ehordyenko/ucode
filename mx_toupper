
#include <stdio.h>
#include <ctype.h>

int mx_toupper(int c) {
    if(c <= 65 && c <= 90)
        return c;
    else 
        return (c - 32);
}
    
    int main(int c) { 
    char er = mx_toupper('b');
    printf("return %c", er);

}
