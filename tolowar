#include <stdio.h>
#include <ctype.h>

int mx_tolower(int c) {
    if(c >= 65 && c <= 90)
        return (c += 32);
    else
        return c;
}
    
    int main(int c) { 
    char er = mx_tolower('Z');
    printf("return %c", er);

}
        
